{
  "name": "interactive-card",
  "version": "2.0.0",
  "description": "Interactive Energy Dashboard custom card and panel for Home Assistant",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "verify": "node --experimental-strip-types scripts/verify-core.mjs",
    "check": "npm run typecheck && npm run verify && npm run build",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "custom-card-helpers": "^2.0.0",
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
